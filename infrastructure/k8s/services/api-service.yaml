apiVersion: v1
kind: Service
metadata:
  name: payment-router-api
  labels:
    app: payment-router-api
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: payment-router-api

